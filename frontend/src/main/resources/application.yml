spring:
  application:
    name: frontend

server:
  port: 8763

eureka:
  client:
    service-url:
      defaultZone: http://peer1:8761/eureka/,http://peer2:8861/eureka/,http://peer3:8961/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
      cors:
        allowed-origins: "*"
        allowed-methods: "*"